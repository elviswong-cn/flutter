import 'package:flutter/material.dart';

class TravelPageView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text('CameraPageView'),
    );
  }
}