import 'package:flutter/material.dart';

class MePageView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text('MePageView'),
    );
  }
}